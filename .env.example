# 服务端口配置
PORT=39100

# Cookie 安全配置
# HTTP 访问时设置为 false, HTTPS 访问时设置为 true
COOKIE_SECURE=false

# Telegram Bot 配置
# 在管理后台配置,或直接在数据库中设置

# 数据库路径 (可选,默认为 ./database/asset-robot.db)
# DATABASE_PATH=./database/asset-robot.db